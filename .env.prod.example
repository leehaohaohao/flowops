# FlowOps 生产环境变量（复制此文件为 .env.prod 并填入真实值）
DB_URL=jdbc:mysql://YOUR_HOST:3306/flowops?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
DB_USERNAME=root
DB_PASSWORD=your_password_here
JWT_SECRET=your_jwt_secret_here

# 本文件只放"应用自身需要的秘密配置"。完整的配置解析顺序见 docs/configuration-loading-order.md：
#   1. 命令行参数（--key=value）
#   2. JVM 系统属性（-Dkey=value）
#   3. 进程环境变量（docker run -e / --env-file / shell export）
#   4. 本文件 .env.<profile>（缺失时降级 .env）
#   5. application-<profile>.yml
#   6. application.yml 与代码内默认值
# 每个变量独立按此顺序查找，命中即止；上层没有该键就自动降级到下一层。
#
# 本文件同时以两种方式进入容器：--env-file 注入为环境变量、-v 挂载到 /app/<文件名> 供应用读取，
# 两种方式的值相同，因此这里的键最终生效于第 3/4 层，都会覆盖 application*.yml。

# 子节点通信监听地址/端口（容器内）由 deploy-prod.sh 用 docker run -e 注入，无需写在本文件：
#   NEXA_MASTER_HOST 默认 0.0.0.0、NEXA_MASTER_PORT 默认 8081（与 application.yml 一致）。
#   -e 的优先级高于本文件，所以要临时改端口请用脚本的 shell 变量覆盖：
#     PORT=8880 NEXA_PORT=8081 NEXA_MASTER_PORT=8081 bash deploy-prod.sh app.jar prod .env.prod
# 宿主机发布端口只由脚本的 -p 决定（PORT 控制台、NEXA_PORT 主从通信），写在本文件里不会改变端口映射。
