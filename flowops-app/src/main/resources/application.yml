# ==========================================
#  FlowOps 公共配置（所有环境共享）
# ==========================================
spring:
  profiles:
    active: prod   # 默认生产环境，启动时可用 --spring.profiles.active=dev 切换
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB

# 服务端口
server:
  port: 8080
  tomcat:
    connection-timeout: 300s
    max-swallow-size: 500MB

# MyBatis-Plus 配置
mybatis-plus:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.nexa.flowops.entity,com.nexa.flowops.permission.entity
  configuration:
    map-underscore-to-camel-case: true

# Sa-Token 配置
sa-token:
  token-name: Authorization
  timeout: 86400
  active-timeout: -1
  is-concurrent: true
  is-share: true
  token-style: jwt
  jwt-secret-key: ${JWT_SECRET}
  is-stateless: true
  is-log: false
  is-read-cookie: true
  is-write-cookie: true

# Docker 配置
docker:
  host: tcp://localhost:2375
  registry-url: https://index.docker.io/v1/

# 产物存储目录
app:
  storage:
    path: /data/flowops/services
  logs:
    path: /data/flowops/logs
    max-file-size: 104857600    # 100MB

# Nexa Protocol Master 配置（分布式子节点通信）
nexa:
  master:
    enabled: true
    # 监听地址：127.0.0.1 仅本机联调可用；跨机器部署必须改为子节点可达的地址
    # （容器内通常用 0.0.0.0，并放通端口与防火墙），通过 NEXA_MASTER_HOST 覆盖
    host: ${NEXA_MASTER_HOST:127.0.0.1}
    port: ${NEXA_MASTER_PORT:9090}
    heartbeat-timeout: 30s
    heartbeat-check-interval: 5s

