# ==========================================
#  FlowOps 生产环境配置（Linux Docker 部署）
#  启动: docker run ... --spring.profiles.active=prod
# ==========================================
spring:
  datasource:
    url: jdbc:mysql://121.40.154.188:3306/flowops?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: lihao123456
    driver-class-name: com.mysql.cj.jdbc.Driver

# 生产环境日志级别
logging:
  level:
    com.nexa.flowops: INFO
